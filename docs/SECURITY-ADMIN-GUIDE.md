# 🔐 Інструкція з безпеки адмін-панелі

## Встановлені захисти

### 1. **Система аутентифікації**
- SHA-256 хешування паролів
- Захист від брутфорсу (3 спроби)
- Автоблокування на 15 хвилин
- Таймаут сесії 60 хвилин

### 2. **Стандартний пароль**
```
BlitzStudio2025!Secure
```
**⚠️ ОБОВ'ЯЗКОВО ЗМІНІТЬ ПАРОЛЬ після першого входу!**

### 3. **Доступ до адмін-панелі**

Адмін-панель доступна за секретним URL, який генерується автоматично.

**Як дізнатись URL:**
1. Відкрийте консоль браузера (F12)
2. Виконайте: `localStorage.getItem('admin_secret_path')`
3. Перейдіть за адресою: `http://yourdomain.com/{secret_path}`

**Приклад:**
```
admin-a3f9k2j8x9c4v7b2n5m1p0q6r8t4w7z9
```

### 4. **Зміна пароля**

**Через консоль браузера:**
```javascript
import { changePassword } from './src/security/adminAuth';

await changePassword('старий_пароль', 'новий_пароль');
```

**Вимоги до пароля:**
- Мінімум 12 символів
- Великі літери (A-Z)
- Малі літери (a-z)
- Цифри (0-9)
- Спецсимволи (!, @, #, $, %, ^, &, *)

### 5. **Захист від ботів**

**robots.txt:**
```
Disallow: /admin*
Disallow: /admin-*
Disallow: /dashboard*
Disallow: /login*
```

**Meta tags:**
- `noindex, nofollow` на всіх адмін-сторінках
- Відсутність посилань з публічних сторінок

### 6. **Логування безпеки**

Всі дії логуються в `localStorage`:
- Спроби входу (успішні/невдалі)
- Зміни конфігурації
- Експорт даних
- Вихід з системи

**Перегляд логів:**
```javascript
JSON.parse(localStorage.getItem('admin_security_events'))
```

### 7. **Рекомендації**

1. **Регулярно змінюйте пароль** (раз на 3 місяці)
2. **Не використовуйте простих паролів** (admin, password, 123456)
3. **Перевіряйте логи безпеки** (вкладка "Безпека" в адмін-панелі)
4. **Не діліться URL адмін-панелі**
5. **Завжди виходьте** після роботи (кнопка "Вихід")
6. **Очищайте сесії** на чужих комп'ютерах

### 8. **Екстрена блокування**

Якщо потрібно терміново заблокувати доступ:

**Метод 1 - Через консоль:**
```javascript
localStorage.removeItem('admin_session');
localStorage.removeItem('admin_password_hash');
localStorage.clear(); // видаляє ВСЕ
```

**Метод 2 - Зміна секретного URL:**
```javascript
localStorage.removeItem('admin_secret_path');
// Після цього згенерується новий URL
```

### 9. **Відновлення доступу**

Якщо забули пароль:

1. Очистіть localStorage:
```javascript
localStorage.removeItem('admin_password_hash');
```

2. Перезавантажте сторінку
3. Використайте стандартний пароль: `BlitzStudio2025!Secure`
4. Змініть пароль одразу!

### 10. **Production налаштування**

Перед deploy:

1. Змініть стандартний пароль в `adminAuth.ts`
2. Додайте HTTPS (обов'язково!)
3. Налаштуйте rate limiting на сервері
4. Додайте IP whitelist (опціонально)
5. Увімкніть 2FA (опціонально)

### 11. **Моніторинг**

**Індикатори атаки:**
- Багато записів "security_alert" в логах
- Заблоковані спроби входу
- Невідомі зміни конфігурації

**Дії при виявленні:**
1. Змініть пароль негайно
2. Очистіть всі сесії
3. Перегенеруйте секретний URL
4. Перевірте логи на підозрілу активність

### 12. **Контакти**

При виявленні вразливостей або проблем:
- Email: security@blitzwebstudio.com
- Telegram: @blitzwebstudio
- GitHub Issues: github.com/blitzwebstudio/security

---

**Останнє оновлення:** 17.12.2025
**Версія:** 1.0.0
