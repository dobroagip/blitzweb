# Quick Deployment Checklist

## ☑️ Перед развертыванием

### 1. Email сервис (ОБЯЗАТЕЛЬНО)
- [ ] Настроен EmailJS (рекомендуется) или
- [ ] Настроен PHP backend или
- [ ] Настроен SendGrid

**Файл для редактирования:** `src/api/emailService.ts`

### 2. SEO настройки
- [ ] Обновлен Google Analytics ID в [index.html](index.html#L8)
- [ ] Добавлено изображение для OG (1200x630px) в `public/og-image.jpg`
- [ ] Проверены meta теги в [index.html](index.html)

### 3. Контактная информация
- [ ] Email для получения сообщений
- [ ] Email для заявок на вакансии

### 4. Админ панель
- [ ] Изменен пароль по умолчанию
- [ ] Получен secret URL: `localStorage.getItem('admin_secret_path')`

---

## 🚀 Команды развертывания

### Сборка проекта
```bash
npm run build
```

### Предпросмотр production версии
```bash
npm run preview
```

---

## 📁 Файлы для загрузки на сервер

### Минимальный набор (только статика):
```
dist/
  ├── index.html
  ├── assets/
  └── robots.txt
```

### С PHP backend:
```
dist/
  ├── index.html
  ├── assets/
  ├── robots.txt
  └── api/
      └── send-email.php
```

---

## 🔧 Настройки хостинга

### Для Apache (.htaccess):
```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

### Для Nginx:
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

---

## ✅ После развертывания

1. **Проверьте функциональность:**
   - [ ] Все страницы открываются
   - [ ] Навигация работает
   - [ ] Формы отправляются
   - [ ] Админ панель доступна

2. **Тестирование форм:**
   - [ ] Отправьте тестовое сообщение
   - [ ] Проверьте получение email
   - [ ] Тестируйте заявку на вакансию

3. **SEO проверка:**
   - [ ] Google Analytics работает
   - [ ] Все meta теги на месте
   - [ ] robots.txt доступен

---

## 📊 Мониторинг

### Логи PHP (если используется):
```bash
tail -f /path/to/api/email_log.txt
tail -f /path/to/api/email_errors.txt
```

### Консоль браузера:
- Откройте DevTools (F12)
- Проверьте вкладку Console на ошибки
- Network - проверьте API запросы

---

## 🔐 Безопасность

- [ ] HTTPS включен
- [ ] Admin панель защищена паролем
- [ ] robots.txt блокирует /admin*
- [ ] Файлы загрузки не публичны

---

Подробные инструкции: [EMAIL-SETUP-GUIDE.md](EMAIL-SETUP-GUIDE.md)
