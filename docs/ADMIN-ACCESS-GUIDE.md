# 🔐 ЯК ОТРИМАТИ ДОСТУП ДО АДМІН-ПАНЕЛІ

## Швидкий старт

### 1. Дізнатись секретний URL

**Метод 1 - Консоль браузера (найпростіший):**
1. Відкрийте ваш сайт в браузері
2. Натисніть `F12` (або `Cmd+Option+I` на Mac)
3. Перейдіть на вкладку **Console**
4. Введіть:
```javascript
localStorage.getItem('admin_secret_path')
```
5. Скопіюйте отриманий шлях (наприклад: `admin-a3f9k2j8x9c4v7b2`)

**Метод 2 - Application Storage:**
1. Натисніть `F12`
2. Вкладка **Application** → **Local Storage** → ваш домен
3. Знайдіть ключ `admin_secret_path`
4. Скопіюйте значення

### 2. Перейти на адмін-панель

Відкрийте в браузері:
```
http://yourdomain.com/admin-a3f9k2j8x9c4v7b2
```

Замініть `admin-a3f9k2j8x9c4v7b2` на ваш секретний шлях.

### 3. Увійти

**Стандартний пароль:**
```
BlitzStudio2025!Secure
```

⚠️ **ОБОВ'ЯЗКОВО ЗМІНІТЬ** після першого входу!

---

## Якщо URL ще не згенеровано

Якщо `admin_secret_path` повертає `null`:

1. Спочатку перейдіть на `/admin`
2. Система автоматично згенерує секретний URL
3. Використайте стандартний пароль для входу
4. Після першого входу перевірте новий URL:
```javascript
localStorage.getItem('admin_secret_path')
```

---

## Безпека

✅ **Що захищено:**
- Секретний URL (32 символи, випадковий)
- SHA-256 хешування паролів
- Захист від брутфорсу (3 спроби → блокування 15 хв)
- Таймаут сесії (60 хвилин)
- robots.txt блокування
- Відсутність посилань з публічних сторінок
- Логування всіх дій

❌ **Що НЕ захищено на клієнті:**
- XSS атаки (потрібен CSP на сервері)
- CSRF атаки (потрібен CSRF token на сервері)
- Man-in-the-middle (потрібен HTTPS)

**Рекомендації:**
1. Завжди використовуйте HTTPS в production
2. Налаштуйте CSP заголовки на сервері
3. Додайте rate limiting на сервері
4. Розгляньте IP whitelist

---

## Консольні утиліти

В консолі браузера доступні зручні команди:

```javascript
// Показати всі команди
adminUtils.help()

// Показати секретний URL
adminUtils.getAdminUrl()

// Скинути пароль
adminUtils.resetPassword()

// Переглянути логи
adminUtils.viewLogs()

// Перевірити сесію
adminUtils.checkSession()

// Вийти
adminUtils.logout()
```

---

## Зміна пароля

**Через адмін-панель:**
1. Увійдіть в адмін-панель
2. Вкладка "Безпека"
3. Розділ "Зміна пароля"
4. Введіть старий та новий пароль

**Через консоль:**
```javascript
import { changePassword } from './src/security/adminAuth';
await changePassword('старий', 'новий');
```

**Вимоги:**
- Мінімум 12 символів
- Великі літери (A-Z)
- Малі літери (a-z)
- Цифри (0-9)

---

## Відновлення доступу

### Забули пароль:
```javascript
localStorage.removeItem('admin_password_hash');
// Використайте стандартний: BlitzStudio2025!Secure
```

### Забули URL:
```javascript
localStorage.getItem('admin_secret_path')
```

### Повне скидання:
```javascript
localStorage.clear();
// Після цього все почнеться з нуля
```

---

## Екстрені дії

### Заблокували себе:
Почекайте 15 хвилин або:
```javascript
localStorage.removeItem('admin_login_attempts');
```

### Підозра на зла мування:
1. Негайно змініть пароль
2. Перегенеруйте URL:
```javascript
localStorage.removeItem('admin_secret_path');
```
3. Очистіть всі сесії:
```javascript
localStorage.removeItem('admin_session');
```
4. Перевірте логи:
```javascript
adminUtils.viewLogs();
```

---

## Контакти підтримки

Якщо потрібна допомога:
- Email: support@blitzwebstudio.com
- Документація: `/SECURITY-ADMIN-GUIDE.md`
- Консольні утиліти: `/admin-console.js`

---

**Останнє оновлення:** 17.12.2025
