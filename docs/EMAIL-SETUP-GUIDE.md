# 📧 Инструкция по настройке отправки Email

## 🚀 Готово для развертывания!

Проект подготовлен для отправки сообщений с контактной формы. Вам нужно выбрать один из методов отправки email.

---

## 📋 Варианты настройки

### ✅ ВАРИАНТ 1: EmailJS (Рекомендуется - БЕСПЛАТНО)

**Преимущества:**
- ✅ Бесплатно до 200 писем/месяц
- ✅ Не требует серверного кода
- ✅ Настройка за 10 минут
- ✅ Работает с любым хостингом (даже статическим)

**Настройка:**

1. **Регистрация:**
   - Перейдите на https://www.emailjs.com/
   - Зарегистрируйтесь бесплатно

2. **Добавьте Email Service:**
   - В панели EmailJS: `Email Services` → `Add New Service`
   - Выберите ваш провайдер (Gmail, Outlook, SendGrid и т.д.)
   - Подключите ваш email

3. **Создайте Email Template:**
   
   **Для обычных сообщений:**
   ```
   Subject: 📧 Нове повідомлення від {{from_name}}
   
   Body:
   Ім'я: {{from_name}}
   Email: {{from_email}}
   Послуга: {{service}}
   
   Повідомлення:
   {{message}}
   ```

   **Для вакансий (создайте отдельный):**
   ```
   Subject: 💼 Заявка на вакансію від {{from_name}}
   
   Body:
   Ім'я: {{from_name}}
   Email: {{from_email}}
   Позиція: {{service}}
   
   Заявка:
   {{message}}
   
   Резюме: {{resume_link}}
   ```

4. **Получите API ключи:**
   - Public Key: `Account` → `General`
   - Service ID: В списке `Email Services`
   - Template ID: В списке `Email Templates`

5. **Обновите конфигурацию:**
   
   Откройте файл `src/api/emailService.ts` и замените:
   ```typescript
   const EMAILJS_CONFIG = {
     PUBLIC_KEY: 'YOUR_PUBLIC_KEY', // Ваш Public Key
     SERVICE_ID: 'YOUR_SERVICE_ID', // Ваш Service ID
     TEMPLATE_ID: 'YOUR_TEMPLATE_ID', // ID шаблона для обычных сообщений
     CAREER_TEMPLATE_ID: 'YOUR_CAREER_TEMPLATE_ID' // ID шаблона для вакансий
   };
   ```

6. **Готово!** 🎉

---

### ✅ ВАРИАНТ 2: PHP Backend (Для собственного сервера)

**Преимущества:**
- ✅ Полный контроль
- ✅ Нет лимитов на количество писем
- ✅ Поддержка прикрепления файлов
- ✅ Уже готовый PHP скрипт

**Требования:**
- PHP 7.4+ с поддержкой mail()
- Веб-сервер (Apache/Nginx)

**Настройка:**

1. **Загрузите на сервер:**
   ```
   public/api/send-email.php → /api/send-email.php
   ```

2. **Настройте права доступа:**
   ```bash
   chmod 755 /path/to/api/send-email.php
   mkdir /path/to/api/uploads
   chmod 755 /path/to/api/uploads
   ```

3. **Измените настройки в send-email.php:**
   ```php
   $CONFIG = [
       'to_email' => 'your-email@yourdomain.com', // ВАШ EMAIL
       'from_email' => 'noreply@yourdomain.com',  // EMAIL С ВАШЕГО ДОМЕНА
       'career_email' => 'hr@yourdomain.com',     // EMAIL ДЛЯ ВАКАНСИЙ
   ];
   ```

4. **Настройте SMTP (если требуется):**
   
   Если стандартная функция `mail()` не работает, установите PHPMailer:
   ```bash
   composer require phpmailer/phpmailer
   ```

5. **Обновите emailService.ts:**
   ```typescript
   // В конце файла src/api/emailService.ts измените:
   export async function sendEmail(data: ContactFormData): Promise<EmailResponse> {
     // Для production используем PHP
     return await sendEmailViaPHP(data);
   }
   ```

6. **Готово!** 🎉

---

### ✅ ВАРИАНТ 3: SendGrid API (Профессиональное решение)

**Преимущества:**
- ✅ 100 писем/день бесплатно
- ✅ Высокая надежность доставки
- ✅ Детальная статистика
- ✅ API для сложных сценариев

**Настройка:**

1. **Регистрация:**
   - https://signup.sendgrid.com/
   - Бесплатный план: 100 писем/день

2. **Получите API ключ:**
   - Settings → API Keys → Create API Key
   - Full Access или Mail Send

3. **Создайте PHP proxy** (из-за CORS):
   
   Создайте файл `public/api/sendgrid-proxy.php`:
   ```php
   <?php
   header('Content-Type: application/json');
   header('Access-Control-Allow-Origin: *');
   
   $apiKey = 'YOUR_SENDGRID_API_KEY';
   $data = json_decode(file_get_contents('php://input'), true);
   
   $ch = curl_init('https://api.sendgrid.com/v3/mail/send');
   curl_setopt($ch, CURLOPT_POST, true);
   curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
   curl_setopt($ch, CURLOPT_HTTPHEADER, [
       'Authorization: Bearer ' . $apiKey,
       'Content-Type: application/json'
   ]);
   curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
   
   $response = curl_exec($ch);
   $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
   curl_close($ch);
   
   echo json_encode([
       'success' => $httpCode === 202,
       'code' => $httpCode
   ]);
   ```

4. **Обновите конфигурацию:**
   ```typescript
   // В src/api/emailService.ts:
   const SENDGRID_CONFIG = {
     API_KEY: 'YOUR_API_KEY',
     FROM_EMAIL: 'noreply@yourdomain.com',
     TO_EMAIL: 'contact@yourdomain.com'
   };
   ```

---

## 🛠 Развертывание на сервер

### Шаг 1: Сборка проекта

```bash
cd /Users/user/Downloads/Images_oil/itnew_2/blizstudio3
npm run build
```

Это создаст папку `dist/` с готовыми файлами.

### Шаг 2: Загрузка на хостинг

**Для статических хостингов (Netlify, Vercel, GitHub Pages):**
```bash
# Загрузите содержимое папки dist/
# EmailJS будет работать автоматически
```

**Для обычного хостинга (cPanel, VPS):**
```bash
# Загрузите содержимое dist/ в public_html/
# Если используете PHP, загрузите также public/api/
```

### Шаг 3: Настройка .htaccess (для Apache)

Создайте файл `.htaccess` в корне:
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

# Защита API
<FilesMatch "\.php$">
  Order Allow,Deny
  Allow from all
</FilesMatch>
```

### Шаг 4: Проверка HTTPS

Убедитесь что ваш сайт работает по HTTPS:
- Бесплатный SSL: Let's Encrypt
- Настройка через cPanel или хостинг-панель

---

## 🧪 Тестирование

### В режиме разработки:
```bash
npm run dev
```
- Форма будет симулировать отправку
- Данные выводятся в консоль браузера

### В production:
1. Откройте сайт
2. Заполните форму
3. Нажмите "Відправити"
4. Проверьте:
   - ✅ Сообщение об успехе появилось
   - ✅ Email пришел на указанный адрес
   - ✅ Форма очистилась после отправки

---

## 📊 Что уже готово:

✅ **Контактная форма с валидацией**
✅ **Форма заявки на вакансию**
✅ **Загрузка файлов резюме (PDF, DOC, DOCX)**
✅ **Ссылка на резюме (Google Drive, Dropbox)**
✅ **Красивые email шаблоны (HTML)**
✅ **Уведомления об успехе/ошибке**
✅ **Защита от спама (в PHP версии)**
✅ **Логирование отправок**
✅ **Dev режим для тестирования**

---

## 🐛 Решение проблем

### Письма не приходят (PHP):
```bash
# Проверьте логи
cat /path/to/api/email_errors.txt

# Проверьте права доступа
ls -la /path/to/api/send-email.php

# Тест mail()
php -r "mail('test@example.com', 'Test', 'Test message');"
```

### CORS ошибки:
- Убедитесь что `Access-Control-Allow-Origin` настроен
- Используйте один и тот же домен для фронтенда и API

### EmailJS не отправляет:
- Проверьте лимиты (200/месяц)
- Убедитесь что API ключи правильные
- Проверьте консоль браузера на ошибки

---

## 📞 Контакты

Если возникли вопросы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Откройте issue на GitHub (если используете)

---

**Готово к запуску! 🚀**

Выберите один из вариантов отправки email и следуйте инструкциям выше.
