# 📧 Email Configuration Guide

## ✅ Що додано:

### 1. **Адмін панель - Email налаштування**
В розділі Overview додано блок "📧 Email налаштування" з двома полями:

- **📩 Контактний Email** - використовується в:
  - Футері сайту (відображається з іконкою)
  - Контактних формах (куди відправляються повідомлення)
  - Заголовках email листів

- **🔒 Privacy Email** - використовується в:
  - Privacy Policy сторінці
  - Розділі "Contact Us" в Privacy Policy
  - Банері внизу Privacy Policy

### 2. **Активні email посилання**
Всі email адреси тепер **активні** - при кліку відкривається email клієнт:

**В Privacy Policy:**
- Email в списку контактів (розділ 5)
- Email в нижньому банері

**У футері:**
- Контактний email з іконкою

### 3. **Футер покращення**
Додано візуальний блок з контактним email:
- 📧 Іконка email
- Текст "Зв'яжіться з нами" / "Contact us"
- Активне посилання на email
- Hover ефект (зміна кольору)

---

## 🛠 Як налаштувати:

### Крок 1: Відкрийте адмін панель
1. Запустіть dev сервер: `npm run dev`
2. Отримайте secret URL:
   ```javascript
   // В консолі браузера:
   localStorage.getItem('admin_secret_path')
   ```
3. Перейдіть за URL: `http://localhost:3001/admin-<secret>`

### Крок 2: Увійдіть
- Пароль за замовчуванням: `BlitzStudio2025!Secure`
- Змініть пароль після першого входу!

### Крок 3: Налаштуйте email
1. На вкладці **Overview** знайдіть блок **"📧 Email налаштування"**
2. Введіть ваші email адреси:
   - **Контактний Email**: `contact@yourdomain.com`
   - **Privacy Email**: `privacy@yourdomain.com`
3. Натисніть **"💾 Зберегти Email налаштування"**

### Крок 4: Перевірте
1. Відкрийте сайт
2. Перейдіть на Privacy Policy сторінку
3. Перевірте що email відображається правильно
4. Клікніть на email - повинен відкритися email клієнт
5. Прокрутіть до футера - побачите новий блок з email

---

## 💾 Де зберігаються дані:

Всі налаштування зберігаються в localStorage браузера:

```javascript
// Контактний email
localStorage.getItem('site_contact_email')

// Privacy email
localStorage.getItem('site_privacy_email')
```

---

## 🔧 Технічні деталі:

### Файли, що були змінені:

1. **src/utils/emailConfig.ts** (НОВИЙ)
   - Утиліти для роботи з email налаштуваннями
   - Функції: `getContactEmail()`, `getPrivacyEmail()`, `createMailtoLink()`

2. **src/pages/admin/AdminDashboard.tsx**
   - Додано стан для email налаштувань
   - Додано UI для редагування email
   - Додано функцію `handleSaveEmailSettings()`

3. **src/pages/PrivacyPolicyPage.tsx**
   - Інтегровано `getPrivacyEmail()`
   - Email тепер активне посилання (mailto:)
   - Автоматично оновлюється з localStorage

4. **index.tsx**
   - Додано email блок у футері
   - Активне посилання з hover ефектом
   - Іконка та стилізація

---

## 📝 Приклади використання:

### В компонентах React:
```typescript
import { getContactEmail, getPrivacyEmail, createMailtoLink } from './utils/emailConfig';

// Отримати email
const contactEmail = getContactEmail();
const privacyEmail = getPrivacyEmail();

// Створити mailto посилання
const link = createMailtoLink(
  contactEmail, 
  'Консультація', 
  'Доброго дня! Хочу замовити сайт...'
);

// В JSX
<a href={`mailto:${contactEmail}`}>
  {contactEmail}
</a>
```

### В адмін панелі:
```javascript
// Оновити налаштування
localStorage.setItem('site_contact_email', 'new-email@domain.com');
localStorage.setItem('site_privacy_email', 'privacy@domain.com');

// Відразу застосовується на всьому сайті!
```

---

## 🎯 Переваги:

✅ **Централізоване управління** - змінюєте в одному місці, застосовується скрізь
✅ **Активні посилання** - користувачі можуть одразу написати лист
✅ **Легка налаштування** - через адмін панель, без коду
✅ **Безпечність** - доступ лише через захищену адмін панель
✅ **Логування** - всі зміни записуються в security events

---

## 🚀 Наступні кроки:

1. **Налаштуйте реальні email адреси** в адмін панелі
2. **Перевірте** що всі посилання працюють
3. **Налаштуйте email відправку** (див. EMAIL-SETUP-GUIDE.md)
4. **Розгорніть на production** (див. DEPLOYMENT.md)

---

**Готово до використання! 🎉**
